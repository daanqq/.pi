---
name: herdr-build
description: Собрать Herdr из текущего checkout и установить бинарник поверх уже используемой локальной версии.
disable-model-invocation: true
---

# Herdr build

1. Найди корень текущего git checkout через `git rev-parse --show-toplevel` и проверь, что `Cargo.toml` описывает пакет `herdr`. Если это не checkout Herdr, остановись и попроси путь; шаг завершён, когда исходники и `command -v herdr` найдены.

2. Разреши путь установленного бинарника через `readlink -f "$(command -v herdr)"`. Выбери Zig в порядке `$ZIG`, `command -v zig`, `$HOME/.local/zig-0.15.2/zig`; если исполняемый Zig не найден, остановись с понятной ошибкой.

3. В корне checkout выполни `ZIG="<путь>" just build`. Не устанавливай бинарник, если сборка завершилась ошибкой; шаг завершён только после успешного release-build в `target/release/herdr`.

4. Установи `target/release/herdr` по разрешённому пути из шага 2. Если пути совпадают, ничего не копируй. Иначе скопируй во временный файл в каталоге назначения через `install -m 755`, затем атомарно замени бинарник через `mv`; не меняй существующую symlink и не используй `sudo` без разрешения пользователя.

5. Проверь, что `readlink -f "$(command -v herdr)"` указывает на установленный файл, его SHA-256 совпадает с собранным бинарником и `herdr --version` выполняется успешно. Сообщи версию и путь. Не перезапускай работающий сервер Herdr: новый бинарник будет использован при следующем запуске.
