# `retro` semantic overlay

## Preserve

- Keep the upstream retrospective workflow and deterministic-check guardrails.
- Load the installed writing guide through `../writing-for-agents/SKILL.md` rather than a nonexistent Skill tool.

## Acceptance

The retrospective proposes improvements to the agent environment without bypassing task permissions, and its recommendations follow the local writing guidance.
