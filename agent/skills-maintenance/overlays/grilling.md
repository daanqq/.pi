# `grilling` semantic overlay

## Preserve

- Keep decision-tree rounds, frontier recomputation, numbered questions, and a recommended answer for every question.
- Use plain ASCII markers (`??` and `=>`) rather than emoji glyphs.
- Show the format for one question without duplicating a second example; the prose already requires the whole frontier.

## Acceptance

Questions whose answers depend on unsettled decisions stay in later rounds, and the agent waits for the user's answers after each frontier.
