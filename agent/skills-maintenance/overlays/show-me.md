# `show-me` semantic overlay

## Preserve

- Keep the upstream visual explanation choices, concise prose, focused HTML artifacts, and file-tree/diagram guidance.
- Open generated HTML with the Pi `bash` tool and `xdg-open` when a graphical session exists; otherwise return the artifact path.

## Acceptance

The skill never emits Claude-style `Bash(...)` tool syntax and still gives the user the smallest useful visual.
