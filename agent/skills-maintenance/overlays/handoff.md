# `handoff` semantic overlay

## Preserve

- Keep the upstream temporary handoff document workflow and its compact reference-first structure.
- In the suggested-skills section, tell the next Pi agent which installed skills to read and use; do not refer to a nonexistent Skill tool.

## Acceptance

The handoff stays outside the workspace, points to existing artifacts instead of duplicating them, and names useful next-session skills in Pi-compatible language.
