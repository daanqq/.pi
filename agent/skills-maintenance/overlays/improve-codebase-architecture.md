# `improve-codebase-architecture` semantic overlay

## Preserve

- Keep the upstream codebase survey, deep-module vocabulary, HTML report, candidate selection, and grilling flow.
- Apply domain-model documentation changes only when permitted by the active task mode and `AGENTS.md`.
- Reach sibling Pi skills through relative file links rather than a nonexistent Skill tool.

## Replace or omit from upstream

- Remove the dependency on the unavailable `domain-modeling` skill.
- Express `CONTEXT.md` updates directly and keep them subject to explicit write authorization.

## Acceptance

The audit remains read-only until the user chooses a candidate and authorizes any resulting documentation or code changes.
